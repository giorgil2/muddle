    <li class="<%= post.css_class %>">
      <a class="timestamp" href="<%= url(:post, post.id) %>">
        <abbr class="updated" title="<%= post.created_at %>"><%= post.updated_at.strftime("%m/%d") %></span>
      </a>
      <%= partial "posts/show/#{post.post_type.name.downcase}", :with => post, :as => :post %>
    </li>