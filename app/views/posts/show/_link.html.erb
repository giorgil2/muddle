  <h2 class="xfolkentry">
    <a class="taggedlink" href="<%= post.link %>"><%= (post.name && !post.name.empty?) ? post.name : post.link %></a>
<%- if post.description && !post.description.empty? -%>
    <small class="description">
      &mdash; <%= post.description %>
    </small>
<%- end -%>
  </h2>